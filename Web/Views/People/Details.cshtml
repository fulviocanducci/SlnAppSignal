@model Web.Models.People
@{
   ViewData["Title"] = "Detalhes";
}
<h1>Pessoa: Detalhes</h1>
<hr />
<div>
   <dl class="row">
      <dt class="col-sm-2 text-end">
         @Html.DisplayNameFor(model => model.Name)
      </dt>
      <dd class="col-sm-10 text-start">
         @Html.DisplayFor(model => model.Name)
      </dd>
   </dl>
</div>
<div class="mt-1">
   <a asp-action="Edit" asp-route-id="@Model?.Id" class="btn btn-primary btn-sm"><i class="bi bi-pencil"></i> Alterar</a>
   <a asp-action="Index" class="btn btn-sm btn-secondary"><i class="bi bi-backspace"></i> Cancelar</a>
</div>
